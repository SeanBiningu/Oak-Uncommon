import React from 'react';
import { Building2, CalendarDays, ClipboardCheck, Earth, ScanLine, Ticket, Users } from 'lucide-react';
import { NavLink } from 'react-router-dom';
import { event, internalNavigation } from '../data/event';

const icons = { register: ClipboardCheck, programme: CalendarDays, partners: Building2, checkin: ScanLine, attendance: Users, ticket: Ticket };
export default function EventShell({ children }) { const navigation = internalNavigation; const navLinks = (className) => navigation.map((item) => { const Icon = icons[item.icon]; return <NavLink key={item.to} to={item.to} end={item.to === '/'} className={({ isActive }) => `${className} ${isActive ? 'active' : ''}`}><Icon /><span>{item.label}</span></NavLink>; }); return <div className="app-shell"><aside className="app-sidebar"><NavLink to="/" className="brand" aria-label="OAK Foundation home"><div className="brand-logo"><span className="brand-o-container">O<Earth className="brand-globe-overlay" /></span><span className="brand-ak">AK</span></div><span className="brand-foundation">FOUNDATION</span></NavLink><p className="convening-name">{event.name.toUpperCase()}</p><nav className="app-nav" aria-label="Event navigation">{navLinks('app-nav-link')}</nav><div className="sidebar-footer">{event.location}<br /><small>{event.dates}</small></div></aside><header className="mobile-event-header"><span className="mobile-oak">OAK</span><span>{event.name.toUpperCase()}</span></header><main className="app-content">{children}</main><nav className="mobile-nav" aria-label="Event navigation">{navLinks('mobile-nav-link')}</nav></div>; }
